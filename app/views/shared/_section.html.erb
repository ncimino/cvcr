<div>
  <%= link_to section.title, section, :class => 'brand' %>
  <table class="table table-hover">
    <tbody>
    <%= render :partial => "shared/section_page",
               :collection => section.pages,
               :as => :page %>
    </tbody>
  </table>
</div>